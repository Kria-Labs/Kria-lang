// Benchmark: Closures
// captured upvalue and repeated calls

set factor = 3
set mult = fn(x) {
    return x * factor
}

set sum = 0
set i = 0

while (i < 50000) {
    set sum = sum + mult(i)
    set i = i + 1
}

print(sum)
